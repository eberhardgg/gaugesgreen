---
import Layout from "../layouts/Layout.astro";
import { ENGAGEMENT_MODEL, IDEAL_CLIENTS } from "../config";
---

<Layout title="Services | Gauges Green" description="Fractional Product & Growth Leadership for venture-backed companies">
  <main class="min-h-screen bg-white dark:bg-[#0a0a0a]">
    <!-- Hero -->
    <section class="pt-12 pb-16 px-6 md:px-8 max-w-2xl mx-auto">
      <h1 class="text-3xl font-semibold text-gray-900 dark:text-white mb-4">
        Services
      </h1>
      <p class="text-base text-gray-600 dark:text-gray-400 leading-relaxed">
        Fractional product & growth leadership. I embed with your team for 4-12 week sprints—hands-on execution alongside your engineers and PMs, not PowerPoints from the sidelines.
      </p>
    </section>

    <!-- What You Get -->
    <section class="pb-12 px-6 md:px-8 max-w-2xl mx-auto">
      <div class="space-y-4">
        <div class="p-6 border border-gray-200 dark:border-gray-800">
          <p class="font-semibold text-gray-900 dark:text-white mb-2">Shipping Infrastructure</p>
          <p class="text-base text-gray-600 dark:text-gray-400">
            Early access tiers, AI-automated release checklists, 48-hour default-to-ship review gates. The systems that make velocity sustainable.
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800">
          <p class="font-semibold text-gray-900 dark:text-white mb-2">Growth Diagnosis</p>
          <p class="text-base text-gray-600 dark:text-gray-400">
            Root cause analysis of why features ship but metrics don't move. Usually diagnosed in the first two weeks.
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800">
          <p class="font-semibold text-gray-900 dark:text-white mb-2">Team Assessment</p>
          <p class="text-base text-gray-600 dark:text-gray-400">
            Honest evaluation of who's in the right seat. Sometimes the answer is "stop hiring"—define the problem before the role.
          </p>
        </div>

        <div class="p-6 border border-gray-200 dark:border-gray-800">
          <p class="font-semibold text-gray-900 dark:text-white mb-2">Hands-On Execution</p>
          <p class="text-base text-gray-600 dark:text-gray-400">
            I ship code alongside your engineers. PRs, not presentations.
          </p>
        </div>
      </div>
    </section>

    <!-- How It Works -->
    <section class="py-12 px-6 md:px-8 max-w-2xl mx-auto">
      <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
        How It Works
      </h2>
      <p class="text-sm text-gray-500 dark:text-gray-500 mb-8">
        {ENGAGEMENT_MODEL.duration} · {ENGAGEMENT_MODEL.commitment}
      </p>

      <div class="space-y-4">
        {ENGAGEMENT_MODEL.phases.map((phase) => (
          <div class="p-6 border border-gray-200 dark:border-gray-800">
            <div class="flex items-center gap-3 mb-2">
              <p class="font-semibold text-gray-900 dark:text-white">{phase.phase}</p>
              <span class="text-sm text-gray-500 dark:text-gray-500">{phase.duration}</span>
            </div>
            <p class="text-base text-gray-600 dark:text-gray-400">
              {phase.description}
            </p>
          </div>
        ))}
      </div>
    </section>

    <!-- Who It's For -->
    <section class="py-12 px-6 md:px-8 max-w-2xl mx-auto">
      <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-8">
        Who It's For
      </h2>

      <div class="space-y-4">
        {IDEAL_CLIENTS.map((client) => (
          <div class="p-6 border border-gray-200 dark:border-gray-800">
            <p class="font-semibold text-gray-900 dark:text-white mb-2">{client.title}</p>
            <p class="text-base text-gray-600 dark:text-gray-400">
              {client.description}
            </p>
          </div>
        ))}
      </div>
    </section>

    <!-- CDMX Office Hours -->
    <section class="py-12 px-6 md:px-8 max-w-2xl mx-auto">
      <div class="p-6 border border-gray-200 dark:border-gray-800">
        <div class="flex items-center gap-3 mb-2">
          <p class="font-semibold text-gray-900 dark:text-white">CDMX Office Hours</p>
          <span class="text-sm text-gray-500 dark:text-gray-500">Free</span>
        </div>
        <p class="text-base text-gray-600 dark:text-gray-400 mb-2">
          Monthly open session for LatAm founders. No pitch, just conversation about product, growth, and entering the US market.
        </p>
        <p class="text-sm text-gray-500 dark:text-gray-500 italic">
          First session: Feb 15, 2026
        </p>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-12 px-6 md:px-8 max-w-2xl mx-auto">
      <div class="p-6 border border-gray-200 dark:border-gray-800">
        <p class="font-semibold text-gray-900 dark:text-white mb-2">Book a diagnostic call</p>
        <p class="text-base text-gray-600 dark:text-gray-400 mb-4">
          30 minutes. No pitch—just diagnosis.
        </p>
        <a
          href="https://cal.com/ryan-eberhard-6jsjkn/30min"
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-primary hover:underline"
        >
          Schedule now →
        </a>
      </div>
    </section>
  </main>
</Layout>
